// Enviro-helper — minimal AMD module. Registers the module in the shell's JS registry so that
// URLs under /Optimizely/DxpEnvironmentIndicator/ are loaded within the shell canvas.
define([], function () { return {}; });
